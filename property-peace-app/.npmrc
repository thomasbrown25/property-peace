# Ensure optional dependencies are installed
# This is critical for Rollup's platform-specific binaries
# Force npm to install optional dependencies even if package-lock.json was generated on a different platform
optional=true
package-lock=true

# Prevent npm workspaces from hoisting these packages
# This ensures metro and expo packages are resolved from local node_modules
shamefully-hoist=false
public-hoist-pattern[]=*expo*
public-hoist-pattern[]=*metro*
public-hoist-pattern[]=*react-native*
